---
phase: 26-io-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/jsonl-reader.ts
  - src/main/jsonl-reader.test.ts
  - src/main/session-detector.ts
  - src/main/session-detector.test.ts
  - src/shared/types.ts
  - src/main/session-store.ts
autonomous: true
requirements: [IO-01, IO-02]
must_haves:
  truths:
    - "Session detection opens each changed JSONL file once per poll cycle, not twice"
    - "discoverSessions returns a Promise and never calls readdirSync, statSync, openSync, readSync, or closeSync"
    - "ENOENT errors during async discovery are caught and do not crash the poll loop"
  artifacts:
    - path: "src/main/jsonl-reader.ts"
      provides: "readSessionTail combining last entry + last tool name in one file open"
      exports: ["readSessionTail", "SessionTailResult"]
    - path: "src/main/session-detector.ts"
      provides: "Async FilesystemSessionDetector using fs.promises"
      exports: ["SessionDetector", "FilesystemSessionDetector"]
    - path: "src/main/session-detector.test.ts"
      provides: "Unit tests for async discovery and ENOENT handling"
    - path: "src/shared/types.ts"
      provides: "SessionDetector interface with Promise return type"
  key_links:
    - from: "src/main/session-detector.ts"
      to: "src/main/jsonl-reader.ts"
      via: "import readSessionTail"
      pattern: "readSessionTail\\(filePath\\)"
    - from: "src/main/session-store.ts"
      to: "src/main/session-detector.ts"
      via: "await this.detector.discoverSessions()"
      pattern: "await this\\.detector\\.discoverSessions\\(\\)"
---

<objective>
Combine the two separate JSONL tail reads (readLastJsonlLine + readLastToolUse) into a single readSessionTail function, then convert the entire session discovery pipeline from synchronous to asynchronous fs.promises calls.

Purpose: Eliminates one redundant file open per changed session per poll cycle (IO-01) and unblocks the main process event loop during filesystem scanning (IO-02).
Output: Async session detection pipeline with combined JSONL tail read, tested with node:test.
</objective>

<execution_context>
@C:/Users/dlaws/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dlaws/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-io-pipeline/26-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/main/jsonl-reader.ts:
```typescript
export interface JsonlEntry {
  type: string;
  sessionId?: string;
  cwd?: string;
  timestamp?: string;
  data?: unknown;
  [key: string]: unknown;
}

export function readLastJsonlLine(filePath: string, bufferSize?: number): JsonlEntry | null;
export function readLastToolUse(filePath: string, bufferSize?: number): string | null;
export async function readUsageTotals(filePath: string): Promise<TokenUsageTotals>;
```

From src/main/session-detector.ts:
```typescript
export interface SessionDetector {
  discoverSessions(): SessionInfo[];
  pruneStaleEntries?(activeSessionIds: Set<string>): void;
}
```

From src/shared/constants.ts:
```typescript
export const JSONL_TAIL_BUFFER_SIZE = 65536; // 64KB
export const IDLE_THRESHOLD_MS = 30_000;
export const STALE_SESSION_MS = 30 * 60 * 1000;
```

From src/main/session-store.ts (line 77):
```typescript
const discovered = this.detector.discoverSessions();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Combined readSessionTail + tests</name>
  <files>src/main/jsonl-reader.ts, src/main/jsonl-reader.test.ts</files>
  <read_first>
    - src/main/jsonl-reader.ts (current readLastJsonlLine and readLastToolUse implementations)
    - src/main/jsonl-reader.test.ts (existing test patterns using node:test + node:assert/strict)
    - src/shared/constants.ts (JSONL_TAIL_BUFFER_SIZE = 65536)
  </read_first>
  <behavior>
    - readSessionTail returns { lastEntry, lastToolName } from a single file open
    - readSessionTail finds lastEntry scanning backward (first valid JsonlEntry with type string)
    - readSessionTail finds lastToolName scanning backward (first assistant entry with tool_use content block)
    - readSessionTail handles empty file (returns { lastEntry: null, lastToolName: null })
    - readSessionTail handles mid-write race (partial last line skipped, falls back to earlier line)
    - readSessionTail handles file with only non-assistant entries (lastToolName is null)
    - readSessionTail handles nonexistent file (returns { lastEntry: null, lastToolName: null })
    - readSessionTail stops scanning early when both lastEntry and lastToolName are found
  </behavior>
  <action>
    1. In `src/main/jsonl-reader.ts`, add the `SessionTailResult` interface and `readSessionTail` async function:

    ```typescript
    export interface SessionTailResult {
      lastEntry: JsonlEntry | null;
      lastToolName: string | null;
    }
    ```

    The function uses `fs.promises.open` + `FileHandle.read()` + `FileHandle.stat()` to read the last `bufferSize` bytes (default JSONL_TAIL_BUFFER_SIZE = 65536). It splits the buffer into lines, then scans backward through ALL lines to find:
    - `lastEntry`: first valid JSON object with `typeof obj.type === 'string'` (scanning backward)
    - `lastToolName`: first assistant entry with a `tool_use` content block whose `name` is a string (scanning backward)

    The backward scan naturally handles mid-write race conditions (partial last line fails JSON.parse, falls back to previous line). Stop scanning early when both `lastEntry !== null && lastToolName !== null`.

    Signature: `export async function readSessionTail(filePath: string, bufferSize: number = JSONL_TAIL_BUFFER_SIZE): Promise<SessionTailResult>`

    Use `fs.promises.open(filePath, 'r')` to get a FileHandle, then `fh.stat()` for size, `fh.read(buffer, 0, readSize, stat.size - readSize)` for the tail buffer, and `await fh.close()` in a finally block.

    Keep `readLastJsonlLine` and `readLastToolUse` exported but mark them with `/** @deprecated Use readSessionTail instead */` -- they are not called anywhere else but removing them now would break imports if any test references them directly.

    Keep `readUsageTotals` unchanged -- it is a separate concern (full file streaming for usage aggregation).

    2. In `src/main/jsonl-reader.test.ts`, add a new `describe('readSessionTail', ...)` block with tests:
    - Empty file returns `{ lastEntry: null, lastToolName: null }`
    - Single assistant entry with tool_use returns both lastEntry and lastToolName
    - File with user + assistant (no tool_use) returns lastEntry but lastToolName is null
    - File with malformed last line falls back to previous valid line
    - Nonexistent file returns `{ lastEntry: null, lastToolName: null }`
    - File with multiple tool_use entries returns the LAST one (scanning backward)

    Use the existing test pattern: `writeTempJsonl` helper, `after()` cleanup, `node:assert/strict`.

    Add a helper to build an assistant entry with tool_use content:
    ```typescript
    function assistantWithToolUse(toolName: string): string {
      return JSON.stringify({
        type: 'assistant',
        message: {
          content: [{ type: 'tool_use', name: toolName, input: {} }],
          model: 'claude-opus-4-6',
          usage: { input_tokens: 10, output_tokens: 5 },
        },
      });
    }
    ```
  </action>
  <verify>
    <automated>npx tsx --test src/main/jsonl-reader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main/jsonl-reader.ts contains `export interface SessionTailResult`
    - src/main/jsonl-reader.ts contains `export async function readSessionTail(`
    - src/main/jsonl-reader.ts contains `fs.promises.open(filePath, 'r')`
    - src/main/jsonl-reader.ts contains `await fh.read(buffer,`
    - src/main/jsonl-reader.ts contains `await fh.close()` or `fh?.close()`
    - src/main/jsonl-reader.ts does NOT contain `openSync` in readSessionTail
    - src/main/jsonl-reader.test.ts contains `describe('readSessionTail'`
    - src/main/jsonl-reader.test.ts contains at least 5 `it(` calls inside the readSessionTail describe
    - `npx tsx --test src/main/jsonl-reader.test.ts` exits 0
  </acceptance_criteria>
  <done>readSessionTail function returns both lastEntry and lastToolName from a single async file open. All existing readUsageTotals tests still pass. New readSessionTail tests cover empty file, tool_use extraction, mid-write fallback, and nonexistent file.</done>
</task>

<task type="auto">
  <name>Task 2: Async session discovery + session-detector tests</name>
  <files>src/main/session-detector.ts, src/main/session-detector.test.ts, src/shared/types.ts, src/main/session-store.ts</files>
  <read_first>
    - src/main/session-detector.ts (current sync discoverSessions, processSessionFile, scanProjectDirectory)
    - src/shared/types.ts (SessionDetector interface location -- line 12-16 area is in session-detector.ts, types.ts has SessionInfo)
    - src/main/session-store.ts (line 77: `const discovered = this.detector.discoverSessions()` call site)
    - src/main/jsonl-reader.ts (readSessionTail from Task 1 -- verify it exists before using)
    - src/shared/constants.ts (IDLE_THRESHOLD_MS, STALE_SESSION_MS, TOOL_TO_ACTIVITY)
  </read_first>
  <action>
    1. **Update SessionDetector interface** in `src/main/session-detector.ts` (lines 12-16):
    Change `discoverSessions(): SessionInfo[]` to `discoverSessions(): Promise<SessionInfo[]>`.
    The interface stays in session-detector.ts (not types.ts -- that is where it currently lives).

    2. **Convert FilesystemSessionDetector to async:**

    a. Change import at top: add `import * as fsp from 'fs/promises';` (keep `import * as fs from 'fs'` for any remaining sync needs like nothing -- actually remove the fs import if fully async, but keep it if `UUID_JSONL_REGEX.test()` etc. still need it). Actually: `fs` import is only needed for types. Switch to: `import { promises as fsp } from 'fs';` and `import type { Dirent } from 'fs';`.

    b. `discoverSessions()` becomes `async discoverSessions(): Promise<SessionInfo[]>`:
    - Replace `fs.existsSync(this.claudeProjectsDir)` with try-catch around `fsp.readdir`
    - Replace `fs.readdirSync(this.claudeProjectsDir, { withFileTypes: true })` with `await fsp.readdir(this.claudeProjectsDir, { withFileTypes: true })`
    - Keep sequential `for...of` for project directories (NOT Promise.all)

    c. `scanProjectDirectory()` becomes `async scanProjectDirectory(...)`:
    - Replace `fs.readdirSync(dirPath, { withFileTypes: true })` with `await fsp.readdir(dirPath, { withFileTypes: true })`
    - Keep sequential `for...of` for entries within directory
    - Wrap in try-catch, catch ENOENT specifically

    d. `processSessionFile()` becomes `async processSessionFile(...)`:
    - Replace `fs.statSync(filePath)` with `await fsp.stat(filePath)`
    - Replace `readLastJsonlLine(filePath)` + `readLastToolUse(filePath)` with `const { lastEntry, lastToolName } = await readSessionTail(filePath)`
    - Update import: `import { readSessionTail, JsonlEntry } from './jsonl-reader';` (remove readLastJsonlLine, readLastToolUse imports)
    - Wrap in try-catch, handle ENOENT (file deleted between readdir and stat)

    e. The `determineStatus()`, `hasToolUseContent()`, and `pruneStaleEntries()` methods remain synchronous (CPU-bound, no I/O).

    3. **Update session-store.ts** call site (line 77):
    Change `const discovered = this.detector.discoverSessions();` to `const discovered = await this.detector.discoverSessions();`
    The `poll()` method is already `async`, so this is a one-line change.

    4. **Create session-detector.test.ts** with tests:
    - Use `node:test` + `node:assert/strict` (same pattern as jsonl-reader.test.ts)
    - Create a temp directory structure mimicking `~/.claude/projects/` with subdirectories containing UUID.jsonl files
    - Test: `discoverSessions()` returns a Promise (verify `result instanceof Promise` before await, or just verify it's awaitable)
    - Test: Returns correct SessionInfo for a valid JSONL file with an assistant entry
    - Test: Handles empty projects directory (returns [])
    - Test: Handles nonexistent projects directory gracefully (returns [])
    - Test: Handles ENOENT when file deleted between readdir and stat (file created, discovered, then deleted before processSessionFile -- use a mock or just verify no crash on missing file)
    - Test: Skips non-JSONL files and subdirectories
    - Test: `determineStatus` returns correct status for each entry type (public method, can test directly)

    Helper to create temp directory structure:
    ```typescript
    function createTempProjectDir(): { baseDir: string; cleanup: () => void } {
      const baseDir = path.join(os.tmpdir(), `test-detector-${Date.now()}`);
      fs.mkdirSync(baseDir, { recursive: true });
      return {
        baseDir,
        cleanup: () => fs.rmSync(baseDir, { recursive: true, force: true }),
      };
    }
    ```
  </action>
  <verify>
    <automated>npx tsx --test src/main/session-detector.test.ts src/main/jsonl-reader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main/session-detector.ts contains `async discoverSessions(): Promise<SessionInfo[]>`
    - src/main/session-detector.ts contains `await fsp.readdir(` or `await fs.promises.readdir(`
    - src/main/session-detector.ts contains `await fsp.stat(` or `await fs.promises.stat(`
    - src/main/session-detector.ts does NOT contain `readdirSync` or `statSync` or `existsSync`
    - src/main/session-detector.ts contains `readSessionTail(` (not readLastJsonlLine or readLastToolUse)
    - src/main/session-store.ts contains `await this.detector.discoverSessions()`
    - src/main/session-detector.test.ts exists and contains at least 4 `it(` test cases
    - `npx tsx --test src/main/session-detector.test.ts` exits 0
    - `npx tsx --test src/main/jsonl-reader.test.ts` exits 0 (no regressions)
  </acceptance_criteria>
  <done>discoverSessions is fully async using fs.promises. No synchronous filesystem calls remain in the session detection path. readSessionTail replaces the two separate read functions. Session-detector tests verify async behavior and ENOENT handling.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsx --test src/main/jsonl-reader.test.ts` -- all readSessionTail + readUsageTotals tests pass
2. `npx tsx --test src/main/session-detector.test.ts` -- all async discovery tests pass
3. Grep verification: `grep -r "readdirSync\|statSync\|openSync\|readSync\|closeSync\|existsSync" src/main/session-detector.ts` returns empty (no sync fs calls remain)
4. Grep verification: `grep "readLastJsonlLine\|readLastToolUse" src/main/session-detector.ts` returns empty (old functions no longer called)
</verification>

<success_criteria>
- Each changed JSONL file triggers exactly one file open (readSessionTail), not two
- discoverSessions returns a Promise and uses only async fs calls
- All existing tests pass with no regressions
- New test files provide coverage for combined read and async discovery
</success_criteria>

<output>
After completion, create `.planning/phases/26-io-pipeline/26-01-SUMMARY.md`
</output>
