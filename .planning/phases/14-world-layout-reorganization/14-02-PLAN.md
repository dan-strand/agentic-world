---
phase: 14-world-layout-reorganization
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/renderer/tilemap-builder.ts
  - src/renderer/building.ts
  - src/renderer/world.ts
autonomous: false
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03]

must_haves:
  truths:
    - "Four workspace buildings visibly fill most of the 1024x768 window in a 2x2 grid"
    - "Guild Hall campfire is a small element at the center crossroads, not a full-sized building"
    - "Dirt paths radiate in a star pattern from campfire to each building entrance"
    - "Paths are narrow (~1 tile wide) giving a footpath feel"
    - "Ground has scattered decorations (rocks, flowers, grass tufts) in gaps"
    - "Labels are positioned inside/over the building scene area, not floating above"
    - "Agents walk to campfire for idle/celebration, walk to buildings for work -- same routing logic"
    - "The entire world fits within 1024x768 with no clipping or overlap"
  artifacts:
    - path: "src/renderer/tilemap-builder.ts"
      provides: "Star pattern path network, ground decorations, 1-tile-wide paths"
      contains: "buildWorldTilemap"
    - path: "src/renderer/building.ts"
      provides: "Building class updated for larger dimensions, label as interior sign"
      contains: "class Building"
    - path: "src/renderer/world.ts"
      provides: "2x2 grid layout with campfire waypoint replacing guild hall building"
      contains: "campfire"
  key_links:
    - from: "src/renderer/world.ts"
      to: "src/shared/constants.ts"
      via: "Uses QUEST_ZONE_POSITIONS for building placement and GUILD_HALL_POS for campfire"
      pattern: "QUEST_ZONE_POSITIONS|GUILD_HALL_POS"
    - from: "src/renderer/world.ts"
      to: "src/renderer/asset-loader.ts"
      via: "Uses campfireTexture for the campfire sprite"
      pattern: "campfireTexture"
    - from: "src/renderer/building.ts"
      to: "src/shared/constants.ts"
      via: "Uses new BUILDING_WIDTH/HEIGHT for label positioning"
      pattern: "BUILDING_WIDTH|BUILDING_HEIGHT"
    - from: "src/renderer/tilemap-builder.ts"
      to: "src/shared/constants.ts"
      via: "Uses QUEST_ZONE_POSITIONS and GUILD_HALL_POS for path routing"
      pattern: "GUILD_HALL_POS|zonePositions"
---

<objective>
Wire the new 2x2 grid layout into the world -- update tilemap paths, building rendering, and world scene composition.

Purpose: Make the world actually display the new layout. Plan 01 created the dimensional constants and sprite assets; this plan connects them to the rendering pipeline so buildings fill the screen and the campfire serves as the central waypoint.

Output: Updated tilemap with star-pattern paths and decorations, Building class adapted for larger size, World.ts using campfire sprite instead of guild_hall building.
</objective>

<execution_context>
@C:/Users/dlaws/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dlaws/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-world-layout-reorganization/14-CONTEXT.md
@.planning/phases/14-world-layout-reorganization/14-01-SUMMARY.md
@src/renderer/tilemap-builder.ts
@src/renderer/building.ts
@src/renderer/world.ts
@src/shared/constants.ts

<interfaces>
<!-- Key types and contracts from Plan 01 output that this plan depends on. -->

From src/shared/constants.ts (after Plan 01):
```typescript
// New building dimensions (landscape, filling 2x2 grid quadrants)
export const BUILDING_WIDTH = ???;   // ~460px (set by Plan 01)
export const BUILDING_HEIGHT = ???;  // ~330px (set by Plan 01)
export const CAMPFIRE_SIZE = 64;

// Positions -- center of each element
export const GUILD_HALL_POS = { x: 512, y: 384 };  // campfire center
export const QUEST_ZONE_POSITIONS = {
  coding:  { x: ???, y: ??? },  // top-left quadrant center (set by Plan 01)
  testing: { x: ???, y: ??? },  // top-right
  reading: { x: ???, y: ??? },  // bottom-left
  comms:   { x: ???, y: ??? },  // bottom-right
};

// Work spots scaled to new building size
export const BUILDING_WORK_SPOTS: Record<BuildingType, WorkSpot[]>;
```

From src/renderer/asset-loader.ts (after Plan 01):
```typescript
export const buildingTextures: Record<string, Texture> = {};  // 4 buildings (no guild_hall)
export const campfireTexture: Record<string, Texture> = {};   // campfire frame
```

From src/renderer/building.ts (current, being modified):
```typescript
export class Building extends Container {
  readonly buildingType: BuildingType;
  constructor(buildingType: BuildingType, texture: Texture);
  setLabel(text: string): void;
  resetLabel(): void;
  getIdlePosition(index: number, total: number): { x: number; y: number };
  getWorkPosition(index: number, total: number): { x: number; y: number };
  getWorkSpot(spotIndex: number): { x: number; y: number };
  getEntrancePosition(): { x: number; y: number };
}
```

From src/renderer/tilemap-builder.ts (current, being modified):
```typescript
export function buildWorldTilemap(
  guildHallPos: { x: number; y: number },
  zonePositions: Array<{ x: number; y: number }>,
  seed?: number,
): Sprite;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tilemap for star-pattern paths and ground decorations</name>
  <files>src/renderer/tilemap-builder.ts</files>
  <action>
Update buildWorldTilemap() in src/renderer/tilemap-builder.ts:

**Path network changes:**
- Replace the current 3-tile-wide paths with 1-tile-wide paths (per user decision: "footpath feel").
- Change path routing: instead of straight lines from guild hall to each zone, draw star pattern -- 4 paths radiating from the central campfire (GUILD_HALL_POS) to the center-bottom edge of each building. The building bottom-center is at approximately { x: zonePos.x, y: zonePos.y + BUILDING_HEIGHT/2 }. Import BUILDING_HEIGHT from constants if needed, or pass building bottom positions as a parameter.
- Path algorithm: keep Bresenham's line but only set the center cell to dirt_path (no dx=-1..1 expansion, just the single tile). This makes 1-tile-wide paths.

**Dirt clearings:**
- Replace the old 5x5 guild hall clearing with a small 3x3 clearing at the campfire center (it is a small waypoint now).
- Remove the 3x3 clearings at zone centers. The buildings cover those areas entirely now -- no dirt clearing needed under buildings that fill their quadrants.
- Instead, add a small 1x2 or 2x1 clearing at each path endpoint (where path meets building bottom) as a "doorstep" effect.

**Ground decorations:**
- After placing all grass and paths, scatter small decorative elements in the grass areas BETWEEN buildings and paths. These are done by placing special tile variants:
  - Add 3 new tile types to the generation script or use existing grass variants with different probability. Alternatively, render decorations directly onto the offscreen canvas as small colored pixels:
    - Small rocks: 2-3px gray clusters
    - Flowers: 1-2px bright color dots (yellow, pink, white)
    - Grass tufts: 2-3px darker green clusters
  - Use seededRandom to deterministically place ~40-60 decorations across the world, but ONLY in grass tiles that are not covered by buildings or paths.
  - To avoid placing decorations under buildings, check if a tile coordinate falls within any building's bounding box (computed from QUEST_ZONE_POSITIONS and BUILDING_WIDTH/BUILDING_HEIGHT). Skip those tiles.

**Keep the function signature compatible** -- it still accepts guildHallPos and zonePositions. If needed, also accept building dimensions or import from constants.
  </action>
  <verify>
    <automated>cd "C:/Users/dlaws/Projects/Agent World" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Paths are 1-tile-wide radiating from campfire center to each building's bottom-center
    - Campfire has small 3x3 dirt clearing (not 5x5)
    - No large clearings at building positions (buildings cover those areas)
    - Ground decorations (rocks, flowers, grass tufts) scattered in grass areas between buildings
    - Decorations avoid building bounding boxes
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Building class and World for campfire waypoint layout</name>
  <files>
    src/renderer/building.ts
    src/renderer/world.ts
  </files>
  <action>
**A. Update src/renderer/building.ts:**

1. **Label positioning:** Per user decision, labels should be "integrated into the interior scene (like a sign or banner inside), not floating above." Change the label from being positioned above the sprite top to being overlaid INSIDE the building, near the top of the building interior. Position the BitmapText at approximately (0, -texture.height * 0.85) or similar so it appears as a banner/sign within the top portion of the building scene. The label should be readable but feel part of the interior scene.

2. **Label styling:** Optionally increase font size slightly since buildings are much larger now. The current 16px may be too small relative to the building. Consider 20-24px. If the BitmapFont supports it at that size, increase it; otherwise keep 16px.

3. **Entrance position:** Update getEntrancePosition() to return a position at the bottom-center of the larger building. Since anchor is (0.5, 1.0) and the building is much taller, the entrance at y=10 below the base is still fine, but verify it looks right with the larger scale. The paths will connect to this point.

4. **Idle position:** Update getIdlePosition() spacing to work with the larger building. The old spacing of 30px was for a 96px building; increase proportionally or use the building width. These positions are only used at the campfire now, so this method mainly matters for the campfire's hosting of idle agents.

5. **Work spot rendering:** The Graphics circles for work spot indicators were sized for 96x96 buildings (4px and 3px circles). These can stay the same size or scale slightly up. They will be replaced in Phase 16 anyway when agents work at stations inside interiors.

**B. Update src/renderer/world.ts:**

1. **Replace guild hall building with campfire sprite:**
   - Import `campfireTexture` from asset-loader.
   - Instead of creating `this.guildHall = new Building('guild_hall', buildingTextures['guild_hall'])`, create a plain PixiJS Sprite from campfireTexture['campfire']. Set anchor to (0.5, 0.5) so it centers on GUILD_HALL_POS.
   - Store as `private campfire: Sprite` instead of `private guildHall: Building`.
   - Position at GUILD_HALL_POS.x, GUILD_HALL_POS.y.
   - Add to buildingsContainer (or a dedicated campfireContainer if z-ordering matters).

2. **Create workspace buildings with new positions:**
   - The loop creating quest zone buildings stays similar but now uses the updated QUEST_ZONE_POSITIONS (which Plan 01 set to 2x2 grid centers).
   - Building sprites come from buildingTextures which no longer has 'guild_hall'.
   - The Building constructor still sets anchor (0.5, 1.0) for bottom-center ground placement. This means the building.position should be set so the bottom-center aligns with the bottom of the quadrant, not the center. Compute: building.position.set(pos.x, pos.y + BUILDING_HEIGHT / 2). OR change the Building anchor to (0.5, 0.5) for center placement and use pos directly. Choose whichever is cleaner -- just ensure buildings are visually centered in their quadrants.

3. **Update agent idle positioning for campfire:**
   - Agents idle at the campfire, not at a guild hall building.
   - The repositionIdleAgents() method currently calls this.guildHall.getIdlePosition(). Since the campfire is now a Sprite (not a Building), implement idle positioning directly in World.ts:
     - Fan idle agents horizontally below the campfire center.
     - Spacing: 30px between agents, centered on campfire.x.
     - Y position: campfire.y + CAMPFIRE_SIZE/2 + 10 (just below the campfire).
   - Update getGuildHallIdlePosition() similarly.

4. **Celebration at campfire:**
   - The celebration animation (golden light column) triggers at agent position, which will be at the campfire. This should work without changes to the celebration code since agents walk back to the campfire position and celebrate there.
   - Verify that startCelebration() and the walk-back-to-HQ flow (assignToHQ) now route to campfire position correctly.

5. **New agent spawn position:**
   - New agents currently spawn at `this.guildHall.x, this.guildHall.y + 20`. Change to `this.campfire.x, this.campfire.y + CAMPFIRE_SIZE/2 + 10`.

6. **Remove guild_hall references:**
   - Remove import of BuildingType where only used for guild_hall typing.
   - Clean up any 'guild_hall' string references that no longer apply.
   - The ACTIVITY_BUILDING constant maps 'idle' -> 'guild_hall' in constants.ts -- this may need updating. If BuildingType no longer includes 'guild_hall', update the idle mapping to a different value or handle idle routing separately in world.ts without relying on ACTIVITY_BUILDING for idle.
  </action>
  <verify>
    <automated>cd "C:/Users/dlaws/Projects/Agent World" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Building labels are positioned inside the building sprite area (not floating above)
    - Campfire sprite renders at world center instead of a guild_hall building
    - Four workspace buildings render at 2x2 grid positions and visually fill the window
    - Idle agents fan out below the campfire
    - Agent creation, celebration, and walk-back all target campfire position
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of new world layout</name>
  <files>none</files>
  <action>
Launch the app and verify the complete world layout reorganization visually. This is a human verification checkpoint -- no code changes needed.

What was built:
- Complete world layout reorganization: 2x2 grid of large workspace buildings with central campfire waypoint, star-pattern footpaths, and scattered ground decorations.

How to verify:
1. Run `npm start` to launch the app
2. Verify the four workspace buildings fill most of the 1024x768 window in a 2x2 grid:
   - Top-left: Wizard Tower
   - Top-right: Training Grounds
   - Bottom-left: Ancient Library
   - Bottom-right: Tavern
3. Verify the central campfire is a small (~64px) element at the crossroads between buildings -- NOT a full-sized building
4. Verify narrow dirt paths radiate from the campfire to each building's bottom-center (star pattern, ~1 tile wide)
5. Verify scattered ground decorations (small rocks, flowers, grass tufts) are visible in gaps between buildings
6. Verify building labels appear INSIDE/OVER the building scene area, not floating above
7. Verify no clipping or overlap -- all buildings fit within the window
8. If any Claude Code sessions are running: verify agents walk to the campfire when idle and to buildings when working; verify celebrations happen at the campfire

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>cd "C:/Users/dlaws/Projects/Agent World" && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User has visually confirmed the new world layout looks correct: 2x2 grid of large buildings, central campfire, star-pattern paths, ground decorations, labels inside buildings, no clipping</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no TypeScript errors)
2. `npm start` launches and displays the new 2x2 grid layout
3. Four large buildings fill the window, campfire is small and central
4. Paths are narrow, radiating from campfire to each building
5. Ground decorations visible in grass areas
6. Agent routing works: idle at campfire, working at buildings
7. Celebrations trigger at campfire position
</verification>

<success_criteria>
- World displays 2x2 grid of large workspace buildings filling most of 1024x768
- Central campfire waypoint replaces guild hall as small element at crossroads
- Star-pattern footpaths connect campfire to all four buildings
- Ground decorations scattered between buildings
- Labels integrated into building scene, not floating
- Agent routing and celebrations work correctly at new positions
- No visual clipping or overlap
</success_criteria>

<output>
After completion, create `.planning/phases/14-world-layout-reorganization/14-02-SUMMARY.md`
</output>
